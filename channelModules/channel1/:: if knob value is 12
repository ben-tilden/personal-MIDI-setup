      // if knob value is 12
      if MIDIByte3 >= 7 and MIDIByte3 <= 12
        tempoPulse = 48
        sendData = [0xCC, 0x03, tempoPulse]
        SendSysex sendData, 3
        sendData = [0x0F, 0xEE, 0xBA, 0x01, 0x0]
        SendSysex sendData, 5
        if knob1Push = 0
          pushReset[MIDIByte2Offset] = 12
        endif
      endif

      // if knob value is 1
      if (MIDIByte3 >= 1 and MIDIByte3 <= 6) or (MIDIByte3 >= 20 and MIDIByte3 <= 25)
        tempoPulse = 36
        sendData = [0xCC, 0x03, tempoPulse]
        SendSysex sendData, 3
        sendData = [0x0F, 0xEE, 0xBA, 0x01, 13]
        SendSysex sendData, 5
        if knob1Push = 0
          pushReset[MIDIByte2Offset] = 1
        endif
      endif

      // if knob value is 14
      if (MIDIByte3 >= 14 and MIDIByte3 <= 19) or (MIDIByte3 >= 33 and MIDIByte3 <= 38)
        tempoPulse = 24
        sendData = [0xCC, 0x03, tempoPulse]
        SendSysex sendData, 3
        sendData = [0x0F, 0xEE, 0xBA, 0x01, 26]
        SendSysex sendData, 5
        if knob1Push = 0
          pushReset[MIDIByte2Offset] = 14
        endif
      endif

      // if knob value is 27
      if (MIDIByte3 >= 27 and MIDIByte3 <= 32) or (MIDIByte3 >= 46 and MIDIByte3 <= 51)
        tempoPulse = 18
        sendData = [0xCC, 0x03, tempoPulse]
        SendSysex sendData, 3
        sendData = [0x0F, 0xEE, 0xBA, 0x01, 39]
        SendSysex sendData, 5
        if knob1Push = 0
          pushReset[MIDIByte2Offset] = 27
        endif
      endif

      // if knob value is 40
      if (MIDIByte3 >= 40 and MIDIByte3 <= 45) or (MIDIByte3 >= 59 and MIDIByte3 <= 64)
        tempoPulse = 12
        sendData = [0xCC, 0x03, tempoPulse]
        SendSysex sendData, 3
        sendData = [0x0F, 0xEE, 0xBA, 0x01, 52]
        SendSysex sendData, 5
        if knob1Push = 0
          pushReset[MIDIByte2Offset] = 40
        endif
      endif

      // if knob value is 53
      if (MIDIByte3 >= 53 and MIDIByte3 <= 58) or (MIDIByte3 >= 72 and MIDIByte3 <= 77)
        tempoPulse = 9
        sendData = [0xCC, 0x03, tempoPulse]
        SendSysex sendData, 3
        sendData = [0x0F, 0xEE, 0xBA, 0x01, 65]
        SendSysex sendData, 5
        if knob1Push = 0
          pushReset[MIDIByte2Offset] = 53
        endif
      endif

      // if knob value is 66
      if (MIDIByte3 >= 66 and MIDIByte3 <= 71) or (MIDIByte3 >= 85 and MIDIByte3 <= 90)
        tempoPulse = 6
        sendData = [0xCC, 0x03, tempoPulse]
        SendSysex sendData, 3
        sendData = [0x0F, 0xEE, 0xBA, 0x01, 78]
        SendSysex sendData, 5
        if knob1Push = 0
          pushReset[MIDIByte2Offset] = 66
        endif
      endif

      // if knob value is 79
      if (MIDIByte3 >= 79 and MIDIByte3 <= 84) or (MIDIByte3 >= 98 and MIDIByte3 <= 103)
        tempoPulse = 4
        sendData = [0xCC, 0x03, tempoPulse]
        SendSysex sendData, 3
        sendData = [0x0F, 0xEE, 0xBA, 0x01, 91]
        SendSysex sendData, 5
        if knob1Push = 0
          pushReset[MIDIByte2Offset] = 79
        endif
      endif

      // if knob value is 92
      if (MIDIByte3 >= 92 and MIDIByte3 <= 97) or (MIDIByte3 >= 111 and MIDIByte3 <= 116)
        tempoPulse = 3
        sendData = [0xCC, 0x03, tempoPulse]
        SendSysex sendData, 3
        sendData = [0x0F, 0xEE, 0xBA, 0x01, 104]
        SendSysex sendData, 5
        if knob1Push = 0
          pushReset[MIDIByte2Offset] = 92
        endif
      endif

      // if knob value is 105
      if (MIDIByte3 >= 105 and MIDIByte3 <= 110) or (MIDIByte3 >= 123 and MIDIByte3 <= 126)
        tempoPulse = 2
        sendData = [0xCC, 0x03, tempoPulse]
        SendSysex sendData, 3
        sendData = [0x0F, 0xEE, 0xBA, 0x01, 117]
        SendSysex sendData, 5
        if knob1Push = 0
          pushReset[MIDIByte2Offset] = 105
        endif
      endif

      // if knob value is 118
      if MIDIByte3 >= 118 and MIDIByte3 <= 122
        tempoPulse = 1
        sendData = [0xCC, 0x03, tempoPulse]
        SendSysex sendData, 3
        sendData = [0x0F, 0xEE, 0xBA, 0x01, 127]
        SendSysex sendData, 5
        if knob1Push = 0
          pushReset[MIDIByte2Offset] = 118
        endif
      endif

    else



      // if knob value is 24
      if MIDIByte3 >= 13 and MIDIByte3 <= 24
        tempoPulse = tempoModeVals[0]
        sendData = [0xCC, 0x03, tempoPulse]
        SendSysex sendData, 3
        sendData = [0x0F, 0xEE, 0xBA, 0x01, 0x00]
        SendSysex sendData, 5
        if knob1Push = 0
          pushReset[MIDIByte2Offset] = 24
        endif
      endif

      // if knob value is 1
      if (MIDIByte3 >= 1 and MIDIByte3 <= 12) or (MIDIByte3 >= 38 and MIDIByte3 <= 49)
        tempoPulse = tempoModeVals[1]
        sendData = [0xCC, 0x03, tempoPulse]
        SendSysex sendData, 3
        sendData = [0x0F, 0xEE, 0xBA, 0x01, 25]
        SendSysex sendData, 5
        if knob1Push = 0
          pushReset[MIDIByte2Offset] = 1
        endif
      endif

      // if knob value is 26
      if (MIDIByte3 >= 26 and MIDIByte3 <= 37) or (MIDIByte3 >= 63 and MIDIByte3 <= 74)
        tempoPulse = tempoModeVals[2]
        sendData = [0xCC, 0x03, tempoPulse]
        SendSysex sendData, 3
        sendData = [0x0F, 0xEE, 0xBA, 0x01, 50]
        SendSysex sendData, 5
        if knob1Push = 0
          pushReset[MIDIByte2Offset] = 26
        endif
      endif

      // if knob value is 51
      if (MIDIByte3 >= 51 and MIDIByte3 <= 62) or (MIDIByte3 >= 88 and MIDIByte3 <= 99)
        tempoPulse = tempoModeVals[3]
        sendData = [0xCC, 0x03, tempoPulse]
        SendSysex sendData, 3
        sendData = [0x0F, 0xEE, 0xBA, 0x01, 75]
        SendSysex sendData, 5
        if knob1Push = 0
          pushReset[MIDIByte2Offset] = 51
        endif
      endif

      // if knob value is 76
      if (MIDIByte3 >= 76 and MIDIByte3 <= 87) or (MIDIByte3 >= 113 and MIDIByte3 <= 126)
        tempoPulse = tempoModeVals[4]
        sendData = [0xCC, 0x03, tempoPulse]
        SendSysex sendData, 3
        sendData = [0x0F, 0xEE, 0xBA, 0x01, 100]
        SendSysex sendData, 5
        if knob1Push = 0
          pushReset[MIDIByte2Offset] = 76
        endif
      endif
      
      // if knob value is 101
      if MIDIByte3 >= 101 and MIDIByte3 <= 112
        tempoPulse = tempoModeVals[5]
        sendData = [0xCC, 0x03, tempoPulse]
        SendSysex sendData, 3
        sendData = [0x0F, 0xEE, 0xBA, 0x01, 127]
        SendSysex sendData, 5
        if knob1Push = 0
          pushReset[MIDIByte2Offset] = 101
        endif
      endif

    endif